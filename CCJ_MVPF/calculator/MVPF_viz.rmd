```{r}
install.packages("ggplot2")
install.packages("treemapify")

library(ggplot2)
library(treemapify)
```

```{r}
#DATA IMPORT

load("MVPF_table.RData")
head(MVPF_table)

```

```{r}
#Create treemap with subgroups

ggplot(MVPF_table, aes(area = value, fill = components, label = value , subgroup=levels)) +
  geom_treemap() +
  geom_treemap_subgroup_border(colour = "white", size = 5) +
  geom_treemap_subgroup_text(place = "centre", grow = TRUE,
                             alpha = 0.25, colour = "black",
                             fontface = "italic") +
  geom_treemap_text(colour = "white",
                    place = "centre",
                    size = 15, grow=TRUE) +
  scale_fill_brewer(palette = "Blues")+
  labs(title="Sub Grouped Tree Plot for MVPF components")

```

```{r}

#Create stacked diverging barchart



```

```{r}